import pandas as pd
import pandera as pa

def extrai_dados(dir_arquivo: str) -> pd.DataFrame:
  pass

def transforma(df:pd.DataFrame) -> pd.DataFrame:
  pass

def carrega_dados(df:pd.DataFrame) -> None:
  pass